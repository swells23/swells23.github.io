import * as screens from '../../../styles/screens.module.scss';

const styles = {
    // [`@media (max-width: ${screens['rem-max']}px)`]: {
    //     root: {
    //         width: 
    //     }
    // }
    root: {
        margin: 'auto',
        maxWidth: screens.remMax
    }
}

export default styles;
